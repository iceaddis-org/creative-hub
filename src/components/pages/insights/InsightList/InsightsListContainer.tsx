import { EventsListPresentation } from '../../events'
import { sampleEvents } from '../../home/events/EventsContainer'

const InsightsListContainer = () => {
  return <EventsListPresentation events={sampleEvents} />
}

export default InsightsListContainer
